{
  "name": "Python & GitHub Actions",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "github.vscode-github-actions"
      ]
    }
  },
  "postStartCommand": "git stash && git pull origin main --rebase && git stash pop || echo 'Workspace was already clean or sync complete.'"
}